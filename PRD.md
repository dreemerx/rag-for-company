开发一个面向企业的RAG智能代理：
开始开发前，你需要明确以下需求：
1.用户群体和场景：
公司内部员工，查公司相关信息（新入职员工快速了解公司文化，快速入职，老员工查找相关信息，领导查看某些信息（高权限）等），需用户认证和权限管理
2.包含哪些工具？
要有鉴权认证系统
-常规权限：
内部知识库（公司制度、流程、手册等）
数据库查询（查业务数据、生成报表）
工单/任务管理（对接 Jira、飞书项目等）
邮件摘要（读取未读邮件，生成摘要）
审批查询（查看/发起自己的审批）
-管理层权限：
团队概览查询（查考勤，项目进度，团队人数）
审批流处理（直接在对话中批准/驳回申请）
注：把工具注册，调用与agent主循环解耦，可以先每种权限实现1-2个工具，把项目跑起来，后面再按需增加工具）
3.高风险操作增加用户确认环节
4.工具调用失败怎么办：
工具调用
  ↓
失败？
  ├─ retryable=True → 自动重试（最多2次，指数退避）
  │     ↓ 仍然失败
  ├─ 有 fallback 工具？→ 切换备用工具
  │     （如：数据库查不到 → 改用知识库检索）
  │     ↓ 没有备用
  └─ 生成友好回复
        ├─ 告知失败原因（人话，不暴露技术细节）
        ├─ 提供替代方案
        └─ 必要时建议联系人工

5.如何评估系统效果？
自制评测集，每次知识库更新或模型切换后，自动跑一遍评测集，准确率不能低于阈值（如85%）。
6.提供云端LLMapi和本地部署两种方案，做可切换
7.做到多用户可同时使用，并且能处理一个session内快速连发消息，但要设置每个用户的资源占用上限（单用户每分钟最多N次请求，单次对话Token上限等）。
8.你自己为这个项目搭配合理前端风格。
9.技术栈选型：
- 向量数据库：Chroma
- 云端LLM：小米MiMo                               
- 本地部署LLM：千问系列（Qwen）
- Agent框架：LlamaIndex
- 前端：React + Ant Design
- 知识库：支持多类型文件导入（PDF、Word、Markdown、TXT等）
- 部署：Docker
- 认证：JWT + RBAC（自建），架构上预留SSO适配器接口，方便后续对接企业飞书/企业微信等

